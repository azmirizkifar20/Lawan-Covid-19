<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.d3if4055.lawancorona">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.about.AboutActivity"></activity>
        <activity android:name=".ui.produktif.EdukasiCovidActivity" />
        <activity android:name=".ui.produktif.BacaTutorialActivity" />
        <activity android:name=".ui.produktif.ProduktifActivity" />
        <activity android:name=".ui.global.DiagnosaMandiriActivity" />
        <activity android:name=".ui.global.StatistikInterActivity" />
        <activity android:name=".ui.global.GlobalActivity" />
        <activity android:name=".ui.provinsi.ProvinsiActivity" />
        <activity android:name=".ui.menu.MenuActivity" />
        <activity android:name=".ui.splash.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>